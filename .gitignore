__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
stocks-venv/
.venv/
venv/
.venv/
.idea/
.vscode/
.databricks/
.env
*.pem
.DS_Store
Thumbs.db
app_generated_files/
# Local DuckDB warehouse — regenerable from scripts/backfill.py, and holds the
# private watchlist's data
warehouse/
# Research and knowledge base — private, and does not travel between devices
knowledge/
.pytest_cache/
notes/
__archive/
_relay.md
# Private watchlist — real list stays local; tickers.example.txt is the public starter
src/common/tickers.txt
# Validation cache — derived, and holds the private symbols
src/common/.tickers_validated.json

# Claude Code — per-device, not shared
.claude/inbox/
.claude/settings.local.json